# AGENTS.md

本 Repository 是獨立的「醫學研究部多單位網站平台」。任何自動化代理或開發者在修改前，必須先閱讀 `README.md`、`PLANS.md` 與 `docs/` 內所有 Markdown 文件。

## 不可違反的範圍

1. 不得修改、刪除、提交或推送原 `ai-center-website-demo`。
2. 不得以 iframe、Git submodule 或執行時依賴連接原人工智慧中心專案。
3. 第一版維持原生 HTML、CSS、JavaScript、JSON；未經明確決策不得引入大型前端框架、正式 CMS、資料庫或登入系統。
4. 不得加入真實帳號、密碼、API Key、病人資料、個資或院內機密。
5. 無法確認的院方資料必須標示「示意內容」或「待院方確認」，不得虛構為正式資訊。

## 資料與權限規則

- 每個單位只能有一筆主要資料；主站與子站必須共用 `data/units.json` 及 Demo 覆寫資料。
- 公開頁面不得顯示 `visibility: internal`，也不得顯示草稿、待確認或封存內容。
- 過期消息保留於歷史列表，不顯示在首頁。
- `owner_unit_id` 在正式系統中只能由後端依登入身分設定。
- 前端角色切換與 `localStorage` 都只是 UI Demo，不得描述為正式安全控制。
- 一般單位角色只可操作自己的單位；醫研部總管理員可跨單位；共用版型與帳號權限只屬系統管理範圍。

## 實作守則

- 優先重用 `assets/css/` 與 `assets/js/` 共用檔案，不為相同功能建立重複版本。
- 新增資料應集中於 `data/`，不要將同一份正式內容散落寫死在多個 HTML。
- 維持繁體中文、清楚語意化標記、可鍵盤操作、明確 focus、表單 label、圖片替代文字及 320px 無整頁水平捲動。
- 新增內部連結時應一併更新連結檢查及相關文件。
- 測試回報只能列出實際執行的檢查；未執行不得寫成通過。

## 變更後最低驗證

1. JSON 可解析。
2. JavaScript 通過語法檢查。
3. HTML 內部連結與本機資源存在。
4. 公開頁面不顯示 `internal` 內容。
5. 單位資料於公開頁與後台預覽同步。
6. 角色功能入口符合 `docs/role-permission-matrix.md`。
7. 桌機與 320px 手機寬度沒有整頁水平捲動，主要互動可用鍵盤完成。
